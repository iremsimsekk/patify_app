server.port=8080
server.error.include-message=always

spring.datasource.url=jdbc:postgresql://aws-1-eu-west-1.pooler.supabase.com:5432/postgres?sslmode=require
spring.datasource.username=postgres.ymyowwtzavkiklowchtd
spring.datasource.password=patifyappTeamasko


spring.jpa.hibernate.ddl-auto=validate
spring.flyway.enabled=true


app.jwt.secret=CHANGE_ME_TO_A_LONG_RANDOM_SECRET_32+CHARS_MINIMUM
app.jwt.expiryMinutes=120

app.emailVerification.base-url=${APP_EMAIL_VERIFICATION_BASE_URL:http://localhost:8080}
app.emailVerification.expiryMinutes=${APP_EMAIL_VERIFICATION_EXPIRY_MINUTES:1440}

spring.mail.host=${SMTP_HOST:smtp.gmail.com}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${SMTP_USERNAME:}
spring.mail.password=${SMTP_PASSWORD:}
spring.mail.properties.mail.smtp.auth=${SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${SMTP_STARTTLS_ENABLE:true}
app.mail.from=${APP_MAIL_FROM:patify.noreply@gmail.com}

# Add here gemini key kopyala yapıştır.!!!!!!!
# gemini.api.key and gemini.api.url should be supplied from env or IDE run config.
# places.api.key is optional and only needed for legacy Google Places import.



